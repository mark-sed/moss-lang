/**
 * Prints Hello, World! to the standard output
 */

print("Hello, World!")

/* Another alternative is to just put the string
 * on the line:
 * `"Hello, World!\n"`
 *
 * This will also work, but only if no special
 * output is chosen, otherwise this will be outputted
 * to the selected output format file as if it was
 * a note.
*/ 